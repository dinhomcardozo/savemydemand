module Providers
    class SessionsController < Devise::SessionsController
      # Você pode personalizar as ações padrão do Devise aqui, se necessário.
      # Por exemplo, sobrescrever o método `new` para alterar a página de login.
    end
  end